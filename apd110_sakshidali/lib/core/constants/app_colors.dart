import 'package:flutter/material.dart';

class AppColors {
  // Brand Core
  static const Color primaryTeal = Color(0xFF1B9AAA); // accents, links
  static const Color tealDark = Color(0xFF16808D); // primary buttons
  static const Color navyCore = Color(0xFF142C52); // headers, navigation

  // Neutral
  static const Color textDark = Color(0xFF071426); // body text
  static const Color background = Color(0xFFF4F7FA); // page background
  static const Color surface = Color(0xFFFFFFFF); // cards, modals

  // Feedback
  static const Color success = Color(0xFF22C55E);
  static const Color successDark = Color(0xFF178740);
  static const Color alert = Color(0xFFEF4444);
  static const Color alertDark = Color(0xFFEB1414);

  static Color? get textPrimary => null;

  static Color? get info => null;
}
